import React from 'react';
import ReactDOM from 'react-dom/client';

const element = <h2>Hello, rendering HTML in React!</h2>;

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(element);
